<script>
    document.getElementById("home").classList.add("active")
</script>
<div class="searchBox">
      <div class="searchTitle">Find an Expert</div>
      <div class="searchBar">
        <input type="text" id="skillSearch" placeholder=" Search..." />
      </div>
</div>
    <div class="tagBoxTitle">Select Tags</div>
    <div class="tagBox" Id="tagBox">
    {{#unless tags}}
        {{#each singleTag}}
        {{> tag}}
      {{/each}}
    {{/unless}}
    
    {{#unless singleTag}}
    
    {{#each tags }}
        {{> tag}}
      {{/each}}
    

    <button class="newTag" id="create-tag-button">+</button>
    </div>
    <div id="modal-backdrop" class="hidden"></div>
    <div id="create-tag-modal" class="hidden">
      <div class="modal-dialog">

        <div class="modal-header">
          <h3>Suggest a new Tag (max 5 characters)</h3>
          <button type="button" class="modal-close-button">&times;</button>
        </div>

        <div class="modal-body">
          <div class="tag-input-element">
            <label for="tag-text-input">Tag name:</label>
            <input id="tag-text-input"></input>
          </div>
        </div>

        <div class="modal-footer">
          <button type="button" class="modal-cancel-button">Cancel</button>
          <button type="button" class="modal-accept-button">Suggest Tag</button>
        </div> 
        </div>
     </div>
    
   
<div class="section">
      <h2>Trending Skills</h2>

      <div class="container">
        <article id="CS361" class="skill">
          <h3>A skill!</h3>
          <p>Could put a description or something here</p>
        </article>
        <article id="CS290" class="skill">
          <h3>Another skill!</h3>
          <p>Could put a description or something here</p>
        </article>
        <article id="CS261"class="skill">
          <h3>Just to keep it going...</h3>
          <p>Could put a description or something here</p>
        </article>
        <article id="HTML" class="skill">
          <h3>Try making the window less wide!</h3>
          <p>They should wrap around :)</p>
        </article>
        <article id="C++" class="skill">
          <h3>Try making the window less wide!</h3>
          <p>They should wrap around :)</p>
        </article>
    </div>
 </div>
    {{/unless}}
    